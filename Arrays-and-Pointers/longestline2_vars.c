#include "longestline2.h"

// Define the global variables - allocate memory storage
int max;		// maximum length seen so far
char line[MAXLINE];	// current input line
char longest[MAXLINE];	// longest line found so far
