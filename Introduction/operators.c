#include <stdio.h>
#include <stdbool.h>

int main(){
    bool the_truth = true;
    bool the_false = false;
    printf("If true, return 1: %d ", the_truth);
    printf("\nIf false, return 0: %d ", the_false);

}